#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print the alphabet in lowercase
 *
 * Return: always 0 (Success)
**/

int main(void)
{
	char x = 'a';

	while (x <= z)
	{
		putchar(x);
		x++;
	}
	putchar('\n');

	return (0);
}
